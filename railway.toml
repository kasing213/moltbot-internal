[build]
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "sh -c 'node dist/index.js onboard --non-interactive --accept-risk --auth-choice openrouter-api-key --skip-daemon --skip-channels --skip-skills --skip-health --skip-ui && node dist/index.js gateway --bind lan --port ${PORT:-18789} --token ${CLAWDBOT_GATEWAY_TOKEN}'"